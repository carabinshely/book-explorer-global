name = "bronerbooks-link-resolver-production"
main = "index.ts"
compatibility_date = "2025-02-01"
# Safe recovery only: deploy this trigger configuration to detach every route from this Worker.
workers_dev = false
preview_urls = false
routes = []
